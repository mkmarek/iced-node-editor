# Basic example

## Running the example

```bash
$ cargo run -p basic_example
```

## Compile and run in browser

Run these commands in the current directory:

```bash
cargo install trunk

trunk serve
```
